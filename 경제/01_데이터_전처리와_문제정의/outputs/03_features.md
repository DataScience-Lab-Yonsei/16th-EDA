# Phase 3 — 피처 엔지니어링

> 자동 생성: `python -m src.run_phase23` · 누수 검증: `python -m src.test_leakage`

## 1. 누수 방지 설계

모든 이력 피처는 **cart 시각 t 보다 엄격히 이전** 이벤트만 반영한다. 이를 관례가 아니라 **구조**로 강제하기 위해 두 장치를 썼다.

1. **동일 시각 붕괴**: 원본 해상도가 1초라 같은 초에 여러 이벤트가 찍힌다. 정렬 후 단순 누적합을 쓰면 *같은 초* 이벤트가 정렬 순서에 따라 우연히 포함된다. 먼저 `(key, event_time)` 으로 집계해 시각당 1행으로 만든 뒤 누적합을 취한다.
2. **`t - 1µs` 로 as-of 조인**: `strategy="backward"` 는 동률을 포함하므로 1마이크로초를 빼서 조인한다. t 시각 이벤트(**지금 이 cart 자신 포함**)가 확실히 배제된다.

> 같은 초에 일어난 구매를 '이전'으로 볼지는 모호한데 **보수적으로 배제**했다. 1초 해상도에서 동시각 이벤트의 선후는 확정할 수 없기 때문이다.

### 검증 결과

`src/test_leakage.py` 가 무작위 cart_line 400건 × 피처 9종을 **원시 이벤트에서 브루트포스로 재계산**해 대조한다.

```
[ OK ] electronics: 3600 검사 전부 통과
[ OK ] cosmetics  : 3600 검사 전부 통과
```

**음성 대조도 함께 수행했다.** `EPS` 를 0 으로 바꿔(= t 시각 이벤트를 포함시켜) 일부러 누수를 주입하면 3,600건 중 **1,600건이 불일치**로 잡힌다. 즉 이 테스트는 통과만 하는 게 아니라 실제로 누수를 탐지한다.

> Phase 1 에서 실제로 누수 사고가 한 번 있었다(패널 전체 구매 수로 티어를 만들어 '미구매 유저 전환율 = 0'). 규칙을 문서로만 두면 반복되므로 테스트로 고정했다.

## 2. 피처 목록

**담기 행동 *(Phase 1 에서 가장 강한 신호)***

| 피처 | 설명 |
|---|---|
| `cart_repeat_cnt` | 동일 (세션×상품) cart 이벤트 수 = **주문 수량 대리변수** |
| `viewed_first` | 같은 세션에서 담기 *이전*에 조회했는가 (검토형 vs 리스트 직행형) |

**오퍼**

| 피처 | 설명 |
|---|---|
| `price` | 담긴 시점 가격 (0 이하는 결측 처리) |
| `price_missing` | 가격 미설정 상품 = 리스팅 품질 대리변수 |
| `price_vs_prior` | 현재가 / 이 상품의 과거 평균가 = **관측 가능한 할인폭** |

**유저 이력**

| 피처 | 설명 |
|---|---|
| `u_views` | 누적 카운트 (t 이전) |
| `u_carts` | 누적 카운트 (t 이전) |
| `u_purchases` | 누적 카운트 (t 이전) |
| `u_removes` | 누적 카운트 (t 이전) |
| `u_spend` | 누적 카운트 (t 이전) |
| `u_orders` | 누적 카운트 (t 이전) |
| `u_tenure_d` | 누적 카운트 (t 이전) |
| `u_recency_d` | 누적 카운트 (t 이전) |
| `u_recency_purchase_d` | 마지막 구매 후 경과일 (미구매 유저는 결측) |
| `u_never_purchased` | t 이전 구매 이력 전무 |
| `u_prior_cvr` | 유저의 과거 cart→purchase 전환율 (라플라스 평활) |
| `u_prior_remove_rate` | 누적 카운트 (t 이전) |
| `u_lines_per_order` | 주문당 라인 수 = **대량구매 성향** (§0.8-F 행동 축) |
| `u_avg_line_value` | 누적 카운트 (t 이전) |

**유저 × 상품**

| 피처 | 설명 |
|---|---|
| `up_carts` | 누적 카운트 (t 이전) |
| `up_purchases` | 누적 카운트 (t 이전) |
| `up_removes` | 누적 카운트 (t 이전) |
| `up_bought_before` | 이 유저가 이 상품을 전에 산 적 있는가 = **재발주 신호** |
| `up_carted_before` | 누적 카운트 (t 이전) |

**상품 이력**

| 피처 | 설명 |
|---|---|
| `p_views` | 누적 카운트 (t 이전) |
| `p_carts` | 누적 카운트 (t 이전) |
| `p_purchases` | 누적 카운트 (t 이전) |
| `p_prior_cvr` | 이 상품의 과거 전환율 |
| `p_prior_price_mean` | 누적 카운트 (t 이전) |

**맥락**

| 피처 | 설명 |
|---|---|
| `s_depth` | 이번 세션에서 담기 전까지의 이벤트 수 |
| `s_elapsed_s` | 세션 시작 후 경과 초 |
| `hour_msk` | 누적 카운트 (t 이전) |
| `dow_msk` | 누적 카운트 (t 이전) |
| `month` | 누적 카운트 (t 이전) |
| `is_weekend` | 누적 카운트 (t 이전) |

## 3. 단변량 신호 강도

![단변량 AUC](figures/04_univariate_auc.png)

| 피처 | AUC | 방향 | 커버리지 |
|---|---:|---|---:|
| `u_prior_cvr` | 0.565 | ↑ 높을수록 전환 | 100.0% |
| `p_prior_cvr` | 0.559 | ↑ 높을수록 전환 | 100.0% |
| `p_purchases` | 0.555 | ↑ 높을수록 전환 | 100.0% |
| `p_carts` | 0.547 | ↑ 높을수록 전환 | 100.0% |
| `u_avg_line_value` | 0.544 | ↑ 높을수록 전환 | 100.0% |
| `u_spend` | 0.543 | ↑ 높을수록 전환 | 100.0% |
| `u_orders` | 0.543 | ↑ 높을수록 전환 | 100.0% |
| `u_purchases` | 0.542 | ↑ 높을수록 전환 | 100.0% |
| `u_never_purchased` | 0.459 | ↓ 높을수록 미전환 | 100.0% |
| `u_lines_per_order` | 0.540 | ↑ 높을수록 전환 | 100.0% |
| `s_elapsed_s` | 0.537 | ↑ 높을수록 전환 | 100.0% |
| `u_recency_d` | 0.535 | ↑ 높을수록 전환 | 97.4% |
| `p_views` | 0.533 | ↑ 높을수록 전환 | 100.0% |
| `cart_repeat_cnt` | 0.530 | ↑ 높을수록 전환 | 100.0% |
| `viewed_first` | 0.529 | ↑ 높을수록 전환 | 100.0% |

> ⭐ **이것이 Phase 3 의 핵심 발견이다: 단변량 신호가 전부 약하다.** 최강 피처가 `u_prior_cvr` AUC **0.565** 이고, 통상 '쓸만하다'고 보는 0.7 근처에 오는 피처가 하나도 없다. electronics 도 마찬가지다(최강 `u_prior_cvr` 0.577). **cosmetics 만의 문제가 아니라, cart 시점에 관측 가능한 정보로는 전환을 잘 맞힐 수 없다는 뜻이다.**

이는 Phase 4~6 에 직접적인 함의를 갖는다:

- Phase 4 모델의 AUC 기대치를 **0.62~0.68 수준**으로 잡는다. 이보다 훨씬 높게 나오면 누수를 먼저 의심해야 한다.
- **정밀 타겟팅의 가치가 제한적**일 수 있다. 누가 전환할지 잘 못 맞히면, 세밀한 세그먼트 타겟팅보다 **넓고 값싼 개입**(리스팅 품질, 발송 타이밍)이 비용 대비 효과가 클 가능성이 있다. Phase 6 제언에서 이 가능성을 열어둔다.

**중복 피처** (|r| ≥ 0.9)

| A | B | 상관 |
|---|---|---:|
| `p_carts` | `p_purchases` | 0.952 |

> SHAP 해석 시 서로의 중요도를 나눠 갖는다는 점을 감안해 읽는다.

## 4. 효과 크기 × 도달 범위 — Phase 1 강조의 정정

![효과와 도달](figures/05_lift_reach.png)

| 조건 | 해당 물량 | 전환율(해당) | 전환율(비해당) | 효과 | 증분 상한 |
|---|---:|---:|---:|---:|---:|
| `multi_qty` | 10.0% | 34.3% | 22.3% | 1.54배 | 521,765 |
| `viewed_first` | 18.6% | 29.1% | 22.2% | 1.31배 | 274,069 |
| `removed_after_cart` | 21.9% | 8.9% | 27.6% | 0.32배 | 197,414 |
| `price_missing` | 0.7% | 3.9% | 23.6% | 0.16배 | 6,261 |

> ⚠️ **Phase 1 §0.8-J 를 정정한다.** `price_missing` 은 전환율을 6.1배 가르지만 **해당 물량이 0.7% 뿐**이라 증분 상한이 6,261건 — 현재 전환 113만 건의 0.6% 다. 고치는 게 옳고 비용도 낮지만 **전략적 레버는 아니다.** Phase 1 에서 효과 크기만 보고 '즉시 실행 가능한 MD 액션으로 승격'이라 쓴 것은 도달 범위를 반영하지 않은 과대평가였다.

> 반대로 `multi_qty` 는 효과(1.54배)와 물량(10.0%)이 모두 받쳐줘 증분 상한이 521,765건으로 가장 크다. Phase 1 의 강조는 이쪽에서는 유효했다.

> ⚠️ **'증분 상한' 은 달성 가능치가 아니다.** 연관성이 전부 인과이고 완전히 닫을 수 있다고 가정한 **상한선**이다. 특히 `multi_qty` 는 구매 의도의 **표식**일 가능성이 높다 — 수량을 늘리게 만든다고 구매가 따라오지는 않는다. 인과 근접 분석은 Phase 5 의 몫이다.

## 5. Phase 4 를 위한 시간 기반 분할

랜덤 분할은 시계열 누수다(미래로 과거를 예측하게 됨). cart 시각 기준으로 자른다.

| 분할 | 기간 (MSK) | cart_line | 전환율 |
|---|---|---:|---:|
| train | 2019-10-01 ~ 2020-01-01 | 2,885,043 | 24.0% |
| valid | 2020-01-01 ~ 2020-02-01 | 972,672 | 24.1% |
| test | 2020-02-01 ~ 2020-03-01 | 968,584 | 21.1% |

> 분할 간 전환율이 21~25% 로 안정적이라 분포 이동은 크지 않다. 다만 2월이 가장 낮으므로 test 성능은 다소 보수적으로 나올 수 있다.
